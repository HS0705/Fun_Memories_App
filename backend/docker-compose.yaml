version: '3'
services: 
    web:
        stdin_open: true
        build:
            context: .
            dockerfile: Dockerfile.dev
        ports:
            - "5000:5000"
        environment:
                - SECRET_KEY
        depends_on:
            - mongodb
    mongodb:
        image: mongo
        volumes:
            - ./data:/data/db
        ports:
            - "27017:27017"