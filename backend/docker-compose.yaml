version: '3'
services: 
    web:
        stdin_open: true
        build:
            context: .
            dockerfile: Dockerfile.dev
        ports:
            - "8080:5000"
        volumes:
            - /app/node_modules
            - .:/app